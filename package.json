{
  "name": "nyx-waitlist-function",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "resend": "^3.2.0"
  },
  "devDependencies": {
    "vitest": "^2.1.0"
  }
}
